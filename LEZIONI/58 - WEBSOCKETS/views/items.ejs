<h1><%= title %></h1> 
<h2>// Questi sono i dati passati dal server iniettati dalla rotta router.get ('/items', (req, res) => {..}</h2>
<ul>
    <% items.forEach(function(item) { %>
        <li><%= item.name %></li>
    <% }); %>   


</ul>